name: agent_ai_calender
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.2.0
  device_calendar: ^4.3.3
  permission_handler: ^12.0.0+1
  http: ^1.4.0
  timezone: ^0.9.0
  firebase_core: ^3.3.0
  firebase_database: ^11.3.7
  cloud_firestore: ^5.6.9
  firebase_auth: ^5.6.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/
